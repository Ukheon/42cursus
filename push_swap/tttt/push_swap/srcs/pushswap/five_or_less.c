#include "../../include/push_swap.h"

void			five_or_less(t_info *info)
{
	// int			i;

	// get_stack_data(info);
	// init_stack(info);
	// info->center_idx = bubble_sort(info, &info->check_arr);
	// info->center_value = info->check_arr[info->center_idx];
	// // info->check_arr => 1 3 2 5 4
	// i = 0;
	// if (info->flag == 0) // stack A에서
	// {
	// 	if (info->remain_arr_count[info->remain_idx] == 5)
	// 	{
	// 		while (1)
	// 		{
	// 			if (check_finish(info, info->stack_a, 5))
	// 			{
	// 				repeat_cmd(info, 5, "ra\n");
	// 				break ;
	// 			}
	// 			if (info->stack_a < info->center_value)
	// 			{
	// 				run_cmd(info, "pb\n");
	// 			}
	// 			i++;
	// 			run_cmd(info, "ra\n");
	// 		}
	// 		repeat_cmd(info, i, "rra\n");
	// 	}
	// }
}